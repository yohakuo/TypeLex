# Wordspell

一个基于 Next.js 15 App Router 的本地优先英语拼写练习应用。

Wordspell 面向听写、章节化练习和错题复习场景，所有数据默认保存在浏览器本地，无需后端服务或账号系统。

## 功能概览

- 本地创建和管理单词本
- 手动添加、编辑、删除单词
- 支持 CSV / TXT 批量导入单词
- 按章节开始听写练习
- 自动记录每章学习进度，并支持下次继续
- 显示整本与分章完成度
- 自动记录听写结果并生成复习队列
- 支持收集今日错题到“错词本”
- 浏览器内发音播放与重播

## 技术栈

- Next.js 15
- React 19
- TypeScript
- Vitest
- localStorage（本地持久化）

## 适用场景

- 学校英语单词听写
- 考试词汇分章练习
- 个人错词回顾与复习
- 离线/本地优先的轻量学习工具

## 快速开始

### 安装依赖

```bash
npm install
```

### 启动开发环境

```bash
npm run dev
```

### 构建生产版本

```bash
npm run build
```

### 启动生产服务

```bash
npm run start
```

### 运行测试

```bash
npm run test
```

### 监听模式运行测试

```bash
npm run test:watch
```

### 运行 lint

```bash
npm run lint
```

## 使用说明

### 1. 创建单词本

进入“管理单词本”页面后，可以创建自己的词书，并设置每章单词数。

### 2. 导入单词

支持两种格式：

#### CSV

首行表头示例：

```csv
word,meaning,example,notes
```

- `word` 必填
- `meaning`、`example`、`notes` 选填

#### TXT

每行一个单词，单词和释义之间使用 Tab 或多个空格分隔，例如：

```txt
emperor   /ˈempərə(r)/ n. 皇帝；君主
```

### 3. 开始听写

选择单词本后，先进入章节列表，再选择具体章节进入听写。

章节页会显示：

- 整本完成进度
- 每章完成数量与百分比
- 是否可以从上次位置继续

### 4. 继续上次进度

应用会自动按“单词本 + 章节 + 每章大小”保存学习进度。

当你再次进入同一章节时：

- 未完成章节：从上次停留位置继续
- 已完成章节：默认从头重新练习，但会保留完成标记

### 5. 复习模式

复习页会显示：

- 已到期的待复习单词
- 最近拼错的单词
- 错词本入口（如果存在）

首页还支持将“今日错题”收集到单独的“错词本”中。

## 听写页快捷键

在 `app/study/[bookId]/run/page.tsx` 对应的听写页面中：

- `Tab`：重播发音
- `↑ / ↓`：切换上一个 / 下一个单词
- `← / →`：在当前输入内容中移动光标
- `Backspace`：删除光标前字符
- `Enter`：提交当前答案并进入下一题

## 数据存储

应用默认使用浏览器 `localStorage` 保存数据，核心入口位于：

- `lib/storage/app-data.ts`
- `providers/app-data-provider.tsx`

当前保存的数据包括：

- 单词本
- 单词列表
- 听写尝试记录
- 复习状态
- 章节学习进度

这意味着：

- 不依赖后端数据库
- 不需要登录
- 清除浏览器本地数据后，学习记录会被清空

## 最近已实现的学习体验优化

- 听写页支持章节级进度持久化
- 章节列表显示整本与分章完成进度
- 再次进入章节时可从上次位置继续
- 左右方向键改为移动输入光标
- 上下方向键用于切换单词
- 修复了快速切词时偶发的双重发音 / 串音问题

## 目录结构

```text
app/                路由页面
components/         共享 UI 组件
features/books/     单词本相关选择器与逻辑
features/study/     听写答案判断逻辑
features/review/    复习调度与选择逻辑
lib/storage/        本地存储与数据模型
lib/speech/         发音播放 hook
lib/csv/            CSV 解析与导出
lib/types/          共享领域类型
providers/          应用数据 Provider
__tests__/          Vitest 纯逻辑测试
```

## 关键页面

- `app/page.tsx`：首页 / 统计概览 / 快捷入口
- `app/books/page.tsx`：单词本列表
- `app/books/[bookId]/page.tsx`：词书编辑与导入
- `app/study/[bookId]/page.tsx`：章节选择与进度展示
- `app/study/[bookId]/run/page.tsx`：听写运行页
- `app/review/page.tsx`：复习队列与最近错词

## 测试建议

当前项目以纯逻辑测试为主，推荐在修改以下模块时优先补测试：

- 答案判断逻辑
- 复习调度逻辑
- CSV 解析
- 学习进度聚合逻辑

## 项目定位

这是一个 MVP 风格的本地优先学习工具，当前重点是：

- 保持实现简单
- 优先浏览器端完成全部功能
- 避免引入后端和账户系统
- 将业务逻辑尽量抽成可测试的纯函数
